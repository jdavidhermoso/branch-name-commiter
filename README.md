# Branch name commiter

This CLI is for executing a GIT commit using the current branch name + two line breaks + commit message provided as argument.

## Usage
```
npm install branch-name-commiter

branch-name-commiter "Commit message"
```

## Commit format
```
#<branch name>

 
<Commit message>
```
